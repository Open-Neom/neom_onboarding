name: neom_onboarding
description: OnBoarding processes for Cyberneom.
version: 1.1.0
homepage: https://github.com/Cyberneom/neom_onboarding
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: https://github.com/Cyberneom/neom_commons.git
  neom_profile:
    git:
      url: https://github.com/Cyberneom/neom_profile.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  neom_commons:
    path: ../neom_commons
  neom_posts:
    path: ../neom_posts
  http: 1.1.0
  uuid: 4.1.0

# The following section is specific to Flutter packages.
flutter:
