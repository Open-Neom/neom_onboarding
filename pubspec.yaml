name: neom_onboarding
description: OnBoarding processes for Open Neom.
version: 1.4.0
homepage: https://github.com/Open-Neom/neom_onboarding
publish_to: 'none'

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Open-Neom/neom_commons.git
  neom_core:
    git:
      url: git@github.com:Open-Neom/neom_core.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
